# Screen capture & OCR
mss>=9.0.0
opencv-python>=4.8.0
Pillow>=10.0.0

# OCR backends (EasyOCR is default, Tesseract is fallback)
easyocr>=1.7.0
pytesseract>=0.3.10

# PyTorch CPU-only (required by EasyOCR, ~200MB)
# If pip installs the GPU version, override with:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0

# Language detection & translation
langdetect>=1.0.9
deep-translator>=1.11.0

# UI overlay & system tray
PyQt5>=5.15.0

# Clipboard monitoring
pyperclip>=1.8.2

# Hotkey support
keyboard>=0.13.5

# Phase 2: UI classifier (lightweight, CPU-only)
scikit-learn>=1.3.0
joblib>=1.3.0
pandas>=2.0.0
